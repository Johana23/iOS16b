import Fundation

print("Números primos del 2 al 400")
var num=Int
var n
var divisor

for var n=2;  n <=400; ++n {
  for var divisor=2; n%divisor !=0; ++divisor;
  if var divisor== n
  print ("{0}|", n)
}

